name: Deploy Specific Folder to Vercel

on:
  push:
    paths:
      - "techbite/**"  # Only trigger when files in 'project-one' change
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deployment for Project One
        run: curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_HQKk6DV2nnPRQM7wVXZPJpvO2LcL/cRJG0lW7gN"
