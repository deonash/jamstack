/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Make sure you don't have any conflicting configuration
  // Remove any experimental app directory features if present
}

module.exports = nextConfig 